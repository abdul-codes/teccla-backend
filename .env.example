# Backend Environment Variables

# ===========================================
# DATABASE
# ===========================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Production: Use your cloud database URL (AWS RDS, Railway, Neon, etc.)
DATABASE_URL=postgresql://postgres:password@localhost:5432/teccla

# IMPORTANT: Previously used TEST_DATABASE_URL for testing.
# Now using DATABASE_URL consistently across all environments.

# ===========================================
# CLOUDFLARE R2 STORAGE
# ===========================================
# Storage provider: 'local', 'r2', or 's3'
# Default: 'r2' for production
STORAGE_PROVIDER=r2

# R2 Account ID (from Cloudflare dashboard)
R2_ACCOUNT_ID=your_cloudflare_account_id

# R2 API credentials (from R2 API Tokens)
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key

# R2 bucket name (created in Cloudflare R2)
R2_BUCKET_NAME=teccla-uploads

# Public URL for accessing uploaded files
# Get this from R2 bucket settings after enabling public access
# Format: https://pub-<hash>.r2.dev OR your custom domain
R2_PUBLIC_URL=https://pub-xxxxxxxxx.r2.dev

# ===========================================
# CLOUDINARY (Optional - for profile pictures)
# ===========================================
# If using Cloudinary for any uploads
# Get from: https://cloudinary.com/console
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# Secret keys for JWT tokens
# IMPORTANT: Generate strong, unique secrets for production
# You can use: openssl rand -base64 32
JWT_ACCESS_SECRET=your_super_secret_access_token_key_minimum_32_characters_long
JWT_REFRESH_SECRET=your_super_secret_refresh_token_key_minimum_32_characters_long

# Token expiration times
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# ===========================================
# CORS & URLS
# ===========================================
# Frontend URL (for CORS)
# Development: http://localhost:5173
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:5173

# Backend URL (for constructing file URLs with local storage)
# Production: https://api.yourdomain.com
BACKEND_URL=http://localhost:8000

# ===========================================
# EMAIL (Optional - if using email features)
# ===========================================
# SMTP configuration for sending emails
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password

# ===========================================
# SERVER
# ===========================================
# Server port
PORT=8000

# Node environment
# Options: 'development', 'production', 'test'
NODE_ENV=development

# ===========================================
# REDIS (Optional - if using Redis for caching/sessions)
# ===========================================
# REDIS_URL=redis://localhost:6379

# ===========================================
# MONITORING & LOGGING (Production)
# ===========================================
# AWS CloudWatch region (if using CloudWatch logging)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret

# Log level: 'error', 'warn', 'info', 'debug'
# LOG_LEVEL=info
